# 运行产物
logs/
__pycache__/
*.pyc
*.pyo

# 模型权重 / 数据（绝不入 git，走 HF / 共享盘 / Modal Volume）
*.pt
*.bin
*.safetensors
*.ckpt
data/
checkpoints/

# 本地环境
.venv*/
.env

# 早期对话导出，只留本地存档，不入仓库
cursor_sglang_lora.md
rl_lora_omni-gpt.md

# 一次性的本地补丁/诊断脚本、临时输出。只匹配仓库根目录，别误伤包内的
# __init__.py 和 _selftest_*.py 之类。
/_*.py
/_*.json
